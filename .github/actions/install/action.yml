on:
  workflow_call:

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - run: corepack enable
      - uses: actions/setup-node@v3
        with:
          node-version-file: ".nvmrc"
          cache: "pnpm"
